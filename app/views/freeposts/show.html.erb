
<p>
  <strong>Title:</strong>
  <%= @freepost.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @freepost.content %>
</p>

  <% if current_user == @freepost.user%>
        <td><%= link_to '수정하기', edit_freepost_path(@freepost) %></td>
        <td><%= link_to '삭제하기', @freepost, method: :delete, data: { confirm: 'Are you sure?' } %></td>
         <% else%>
         <td></td>
         <%end%>
<%= link_to 'Back', freeposts_path %>
